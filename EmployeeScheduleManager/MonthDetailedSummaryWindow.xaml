<Window x:Class="EmployeeScheduleManager.MonthDetailedSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Miesięczne Podsumowanie" Height="700" Width="900">
    <Grid>
        <DataGrid x:Name="PodsumowanieDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Dzień" Binding="{Binding Dzien}" />
                <DataGridTextColumn Header="Łączne godziny" Binding="{Binding LacznaLiczbaGodzin}" />
                <DataGridTextColumn Header="Liczba pracowników" Binding="{Binding LiczbaPracownikow}" />
                <DataGridCheckBoxColumn Header="Pokrycie pełne" Binding="{Binding PokrycieOdOtwarciaDoZamkniecia}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
